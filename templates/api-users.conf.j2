// {{ ansible_managed }}
{% for item in i2_api_users|dict2items %}
object ApiUser "{{ item.key }}" {
   password = "{{ item.value.password }}"
{% if item.value.permissions is defined %}
   permissions = [ "{{ item.value.permissions | join('", "') }}" ]
{% endif %}
}
{% endfor %}
